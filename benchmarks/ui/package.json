{
  "name": "emissary-benchmark-ui",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright before/after capture of the Emissary 'Ask Anything' UI. Standalone — not part of the app bundle.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
