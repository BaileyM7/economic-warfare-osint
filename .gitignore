# Environment
.env
.env.local
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/
.coverage
.coverage.*
htmlcov/
pip-wheel-metadata/

# Cache
data/cache/

# IDE
.idea/
.vscode/
.cursor/
*.swp

# Logs
*.log

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Playwright MCP browser cache (diagnostic artifacts, not source)
.playwright-mcp/

# Local SQLite database (regenerated by tests / dev runs)
data/emissary.db

# Local notification stub logs (runtime "would-send" records)
data/sendgrid_stub.jsonl
data/twilio_stub.jsonl

# Design reference assets (brand kit, not application code)
Agile Defense Design System/

# Diagnostic screenshots from manual smoke tests
vessel-track-*.png
wargame-loads.png

# Benchmark run artifacts (health-stamped result sets, reports, videos) — the
# harness code is committed, the outputs are local/ephemeral.
benchmarks/results/
benchmarks/ui/node_modules/
benchmarks/ui/playwright-report/
benchmarks/ui/test-results/

# Vendored auto-loom skill (demo-videos recording engine): commit the scripts,
# not its installed deps or recording outputs.
.claude/skills/auto-loom/node_modules/
.claude/skills/auto-loom/demo-video-out/

# Local scratch / temp working files
FRAGILITY_METHODOLOGY.tmp.md
